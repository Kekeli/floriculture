
<div id="list">
	
	<table class="table table-striped table-bordered table-condensed">
		<thead>
			<tr>
				<th></th><th>Family</th><th>Genus</th><th>Species</th><th>Variety</th><th>Common Name</th><th>Section</th><th>Bench</th><th>Action</th>
			</tr>
		</thead>
		<tbody>
			<% plants.forEach( function ( plant ){ %>
				<tr>
					<td><a class="update-link" href="/plants/<%= plant._id %>" title="Display this plant"><img src="/<%= plant.getImageUrl() %>" height="40" width="30"/></a></td>
					<td><%= plant.Family %></td>
					<td><%= plant.Genus ? plant.Genus : '' %></td>
					<td><%= plant.Species ? plant.Species : '' %></td>
					<td><%= plant.Variety ? plant.Variety : '' %></td>
					<td><%= plant.Common_Name ? plant.Common_Name : '' %></td>
					
					<td><%= plant.Section ? plant.Section : ''%></td>
					<td><%= plant.Bench ? plant.Bench : ''%></td>
					<td>
						<a class="btn btn-mini" href="/plants/<%= plant._id %>" title="Details">Details</a>
						<a class="btn btn-mini" href="/plants/edit/<%= plant._id %>" title="Edit" disabled="true">Edit</a>
    					<!--<a class="btn btn-small btn-danger" href="/plants/destroy/<%= plant._id %>" title="Delete">Delete</a>-->
    				</td>
   				</tr>
			<% }); %>
		</tbody>
	</table>
	
	<%- plants.pagination.render({ baseUrl: '/plants' }) %>

	<!-- todo: display only for editor roles -->
	<a class="btn btn-small btn-primary pull-right" href="/plants/new" disabled="true">Add</a>
	<br />
</div>